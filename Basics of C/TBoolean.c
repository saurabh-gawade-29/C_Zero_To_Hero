#include <stdio.h>
#include <stdbool.h>

int main()
{
    bool a = true; // do not use double quotes for boolean values, use true or false instead
    bool b = false;
    printf("%d\n", a);
    printf("%d\n", b);
    return 0;
}