#include <stdio.h> // import standard input/output library

int main() // main function, entry point of the program
// Escape sequence: \n (newline), \t (tab), \\ (backslash), \" (double quote), \' (single quote)
{
    printf("Twinkle Twinkle Little Star How I Wonder What You Are"); // print "Hello World" to the console
    return 0;                                                        // return 0 indicates that the program has executed successfully
}
// This is a single-line comment. It is used to provide brief explanations or notes about the code. Single-line comments start with // and continue until the end of the line.
/*
  This is a multi-line comment.
  It can span multiple lines and is used to provide detailed explanations or documentation for the code.
  Multi-line comments start with /* and end with *\/ (without the backslash).
 */
